const Footer = () => {
  return (
    <footer className="text-center py-2 text-xl font-mono text-black">
      © {new Date().getFullYear()} Michael Parascandolo
    </footer>
  );
};

export default Footer;
